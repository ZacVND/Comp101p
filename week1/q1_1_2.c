#include <stdio.h>
#include <string.h>

int main(void)
{
  char name[] = "Dept. of Computer Science";
  char address[] = "Malet Place Engineering Building";
  printf("Send this to %s, %s\n", name, address );
  
  /* int index = strlen(myName) - 1;
  while (index > -1)
    {
      printf("%c", myName[index]);
      index = index -1;
    }
    printf("\n");*/
  return 0;
}
